{
  // Everything the build config covers, plus the tests. This is what
  // `pnpm typecheck` runs, so a broken test is a failed type-check.
  "extends": "./tsconfig.json",
  // Not composite: a project that emits nothing cannot be one.
  "compilerOptions": { "composite": false, "noEmit": true },
  "exclude": []
}
