{
  // Build config: emits dist/, and leaves the tests and their helpers out of it.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",

    // Referenced by apps/web, which types its oRPC client from `ApiRouter`, so
    // its build and type-check rebuild this one rather than read a stale dist.
    "composite": true,

    "module": "nodenext",
    "target": "esnext",
    "lib": ["esnext"],
    "types": ["node"],

    "sourceMap": true,
    "declaration": true,
    "declarationMap": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "src/test"],
  "references": [{ "path": "../../packages/tmdb" }]
}
